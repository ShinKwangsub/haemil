package runtime

import "testing"

// TestRunTurnSkeleton is a placeholder. RunTurn is a panic stub in Phase 2
// and replacing it with a real test requires wiring a fake Provider + fake
// Tool, which arrives in Phase 2b together with the conversation loop
// implementation.
func TestRunTurnSkeleton(t *testing.T) {
	t.Skip("TODO: implement once RunTurn body is filled in (Phase 2b)")
}
